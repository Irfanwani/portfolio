import { Box, Text } from "@chakra-ui/react";

const Home = () => {
  return (
    <Box flex={1} height={window.innerHeight}>
        <Text className="">Irfan wani</Text>
    </Box>
  );
};

export default Home;
